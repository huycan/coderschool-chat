<div class="container">
	<h4>Available users</h4>
	<% @users.in_groups_of(3) do |group| %>
	  <div class="row">
	    <%= render partial: 'user', collection: group.compact %>
	  </div>
	<% end %>
</div>
